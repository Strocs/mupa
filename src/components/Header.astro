---
import Logo from "./Logo.astro";
import Nav from "./Nav.astro";

const { logo = true } = Astro.props;
---

<header class="w-full h-14 sm:h-20 px-6 sm:px-12">
  {logo && <Logo />}
  <Nav />
</header>
