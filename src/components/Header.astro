---
import Logo from "./Logo.astro";
import Nav from "./Nav.astro";

const { logo = true } = Astro.props;
---

<header class="w-full h-20 md:h-20 px-6 md:px-12 flex items-center">
  {logo && <Logo />}
  <Nav />
</header>
